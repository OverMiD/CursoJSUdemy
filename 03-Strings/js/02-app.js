const producto = "Monitor 20";

// length -- Devuelve un entero con la cantidad de caracteres que contiene la variable
console.log(producto.length);

// indexOf() -- Devuelve la posición inicial en la que se encuentra el texto indicado
// pero devuelve -1 si no lo encuentra
console.log(producto.indexOf("Monitor"));

// includes() -- Devuelve true o false dependiendo si encuentra o no el texto dentro de la variable
console.log(producto.includes("Monitor"));