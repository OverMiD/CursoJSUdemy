const producto = {
    nombre: "monitor 20 pulgadas",
    precio: 300,
    disponible: true
}

// Forma de acceder a las propiedades del objeto
console.log(producto.nombre);
console.log(producto["nombre"]);